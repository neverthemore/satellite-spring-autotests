# Адрес тестируемого приложения (satellite-spring) по умолчанию.
# Переопределить можно без правки этого файла:
#   1) флагом Gradle:        ./gradlew test -Dbase.url=http://localhost:9090
#   2) переменной окружения: BASE_URL=http://localhost:9090 ./gradlew test
# Приоритет: -Dbase.url > $BASE_URL > это значение > http://localhost:8080
base.url=http://localhost:8080
